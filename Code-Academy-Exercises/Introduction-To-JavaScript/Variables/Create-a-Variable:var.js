/*to create a variable we use the var keyword to declare the variable with the variable name(which should not be a keyword or start with a number)
 use the assignment operator to assign it a value then initialize it with a value*/

var favoriteFood = 'pizza';
var numOfSlices = 8;
console.log(favoriteFood + ' ' + numOfSlices + ' slices');
